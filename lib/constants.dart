import 'package:flutter/material.dart';

const blackcontainerStyle = BoxDecoration(
  color: Colors.black,
  borderRadius: BorderRadius.only(
    bottomLeft: Radius.circular(10.0),
    bottomRight: Radius.circular(10.0),
  ),
);

const buttontextColor1 = Color(0xff888888);
const buttontextColor2 = Colors.blueAccent;
const buttontextColor3 = Colors.white;
const buttonbackgroundColor1 = Colors.white;
const buttonbackgroundColor2 = Color(0xff89d1d6);
const buttonPadding1 =
    EdgeInsets.only(left: 5.0, right: 5.0, bottom: 10, top: 10);
const buttonPadding2 =
    EdgeInsets.only(left: 50.0, right: 50.0, bottom: 15, top: 15);
const buttonWeight = FontWeight.w700;
